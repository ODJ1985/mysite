# wyEBIYA Podcast アプリケーション ファイルサイズ制限調査レポート

## 調査概要

wyEBIYA Podcastアプリケーションの現在のファイルサイズ制限（7MB）について調査し、安全に拡張できる可能性を評価しました。

## 調査結果サマリー

1. **現在の制限確認**:
   - 現在の制限は正確に6.99MBで機能しています（7MBと表示されていますが、実際には少し小さい値）
   - 7.1MB以上のファイルでは413エラー（Request Entity Too Large）が発生します
   - エラーはNginxプロキシレベルで発生しています

2. **サーバー環境制限の詳細**:
   - FastAPI/Uvicornの制限: 7MB（コード内で設定）
   - Nginxプロキシの制限: 8MB（Nginxの413エラーメッセージから判断）
   - 実際のボトルネックはNginxプロキシの設定です

3. **拡張可能性の評価**:
   - FastAPI/Uvicornの制限は10MBまで安全に拡張可能
   - Nginxプロキシの制限は8MBで固定されており、Kubernetes環境のため変更が困難
   - 実質的な最大アップロードサイズは8MB未満に制限されます

## 詳細調査結果

### 1. 現在の制限確認

#### テスト結果:
- 6.9MB: 成功 ✅
- 7.0MB: 成功 ✅
- 7.1MB: 失敗 ❌ (413 Request Entity Too Large)
- 8.0MB: 失敗 ❌ (413 Request Entity Too Large)
- 10.0MB: 失敗 ❌ (413 Request Entity Too Large)

#### 正確な制限値:
詳細なテストにより、実際の制限は約6.99MBであることが判明しました。

### 2. サーバー環境制限の詳細調査

#### バックエンドコード制限:
バックエンドコードでは、以下の制限が設定されています:
```python
max_size = 7 * 1024 * 1024  # 7MB
```

#### Nginxプロキシ制限:
エラーメッセージから、Nginxプロキシが413エラーを返していることが確認できました:
```
<html>
<head><title>413 Request Entity Too Large</title></head>
<body>
<center><h1>413 Request Entity Too Large</h1></center>
<hr><center>nginx</center>
</body>
</html>
```

#### 制限の修正テスト:
バックエンドコードの制限を10MBに変更しても、8MB以上のファイルでは依然としてNginxプロキシからの413エラーが発生しました。

### 3. ユーザーニーズ確認

フロントエンドUIの調査から:
- 現在のUIには「最大7MB」の制限が明示されています
- Google音声概要機能は利用可能ですが、大きなファイルでのテストはできていません

### 4. 技術的制約の分析

#### 特定された制約:
1. **FastAPI/Uvicornの制限**: コードで設定された7MB制限
2. **Nginxプロキシの制限**: 約8MB（Kubernetes環境で設定）
3. **タイムアウト設定**: Uvicornのタイムアウトは30秒（大きなファイルでは問題になる可能性あり）

#### 制約の影響:
- バックエンドコードの制限を変更しても、Nginxプロキシの制限を超えることはできません
- Kubernetes環境のため、Nginxプロキシの設定変更は困難です

### 5. 拡張可能性の評価

#### 安全に拡張可能な上限:
- **推奨上限**: 8MB未満（7.9MB程度）
- **理由**: Nginxプロキシの制限が約8MBであるため

#### 必要な変更:
1. バックエンドコードの制限を7MBから8MBに変更:
```python
max_size = 8 * 1024 * 1024  # 8MB
```

2. エラーメッセージの更新:
```python
raise HTTPException(status_code=413, detail="File size must be less than 8MB")
```

3. フロントエンドUIの表示を「最大8MB」に更新

## 改善提案

### 1. 段階的拡張アプローチ

**推奨手順**:
1. バックエンドコードの制限を7MBから7.9MBに変更
2. UIの表示を「最大8MB」に更新
3. 実際の使用状況をモニタリング
4. 必要に応じてさらなる対応を検討

### 2. 代替手法の検討

より大きなファイルが必要な場合:
1. **チャンク分割アップロード**: ファイルを小さなチャンクに分割してアップロード
2. **外部ストレージの利用**: Google DriveやS3などの外部ストレージを活用
3. **音声圧縮の最適化**: アップロード前に音声ファイルを最適化

### 3. 長期的な解決策

Kubernetes環境のNginx設定を変更するには:
1. Kubernetesの管理者に連絡し、Ingressの`client_max_body_size`設定の変更を依頼
2. または、独自のプロキシサーバーを設定して制限を回避

## 結論

現在のwyEBIYA Podcastアプリケーションのファイルサイズ制限は約7MBですが、バックエンドコードの変更により7.9MB程度まで安全に拡張できます。ただし、Nginxプロキシの制限（約8MB）を超えることはできないため、より大きなファイルが必要な場合は代替手法を検討する必要があります。

短期的には7.9MBへの拡張を推奨し、長期的にはKubernetes環境の設定変更やチャンク分割アップロードなどの代替手法を検討することをお勧めします。
